# Full Dataset Iteration 5 Big RO Test

This directory is an empty placeholder from the frozen exploration period. It
does not contain a committed dataset-generation source package or canonical
result manifest.

For PR review, the canonical big-RO extension is
`examples/ml_deep_bitstream_inspection/datasets/full_dataset_it4_big_ro`. Keep this placeholder only if it is useful
as a signpost for historical naming; otherwise it can remain untracked or be
removed from the PR surface without affecting reproducibility.
